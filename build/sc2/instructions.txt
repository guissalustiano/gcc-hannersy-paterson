# sc2: rv32i — fence absent, everything else present

# arithmetic
add
addi
sub
neg     # sub rd, x0, rs
mul     # not in rv32i but harmless to list

# logic
and
andi
or
ori
xor
xori
not     # xori rd, rs, -1

# shifts
sll
slli
srl
srli
sra
srai

# load / store
lw
sw
lh
lhu
lb
lbu
sh
sb

# upper immediate / PC-relative
lui
auipc

# jump / call
jal
jalr
jr      # jalr x0, 0(rs)
ret     # jalr x0, 0(ra)
call    # auipc+jalr pseudo
tail    # auipc+jalr pseudo (tail call)

# branches
beq
bne
blt
bge
bltu
bgeu
beqz    # beq rs, x0, offset
bnez    # bne rs, x0, offset
bltz    # blt rs, x0, offset
bgez    # bge rs, x0, offset
blez    # bge x0, rs, offset
bgtz    # blt x0, rs, offset
ble     # bge rs2, rs1, offset
bgt     # blt rs2, rs1, offset
bleu    # bgeu rs2, rs1, offset
bgtu    # bltu rs2, rs1, offset

# comparison / set
slt
slti
sltu
sltiu
seqz    # sltiu rd, rs, 1
snez    # sltu rd, x0, rs
sltz    # slt rd, rs, x0
sgtz    # slt rd, x0, rs

# pseudo load / move
li
mv
la      # auipc+addi pseudo

# NOP
nop     # addi x0, x0, 0
