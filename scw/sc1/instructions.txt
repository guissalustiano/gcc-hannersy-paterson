# sc1: rv32i subset — lw sw beq add addi sub and or jalr lui
# Real binary opcodes only; pseudo-instructions are expanded by the assembler:
#   neg → sub    mv → addi    jr/ret → jalr    li → addi or lui+addi
#   ble → bge    snez → sltu

# arithmetic
add
addi
sub

# logic
and
or

# load / store
lw
sw

# upper immediate / jump
lui
jalr

# branch
beq
bge     # ble pseudo (loop bounds check) expands to bge rs2, rs1, offset

# comparisons (used in blt/bge/bltu/bgeu branch synthesis)
slt
sltu
