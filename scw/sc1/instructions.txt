# sc1: rv32i subset — lw sw beq add addi sub and or jalr lui
# Shifts, auipc, fence absent. bne/xor synthesized via available primitives.
#
# Pseudo-instructions below expand to real instructions at assemble time;
# they are listed here because GCC emits them in -S output.

# arithmetic
add
addi
sub
neg     # sub rd, x0, rs

# logic
and
or

# load / store
lw
sw

# upper immediate / jump
lui
jalr
jr      # jalr x0, 0(rs)
ret     # jalr x0, 0(ra)

# branch
beq
ble     # bge rs2, rs1, offset  (loop bounds check)

# pseudo load / move
li      # lui+addi or addi x0,imm
mv      # addi rd, rs, 0

# comparison
# TODO: remove slt/sltu once blt/bge/bltu/bgeu synthesis no longer relies on
# these instructions (i.e., after a native slt-free synthesis is implemented).
slt     # signed less-than (used in blt/bge synthesis)
sltu    # unsigned less-than (used in bltu/bgeu synthesis)

# comparison pseudo
snez    # sltu rd, x0, rs
